<div style="display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; padding: 200px 0 20px 0;">

<div style="font-size: 24px; margin-bottom: 12px;"><strong>Amazon Q</strong></div>
<div style="background: rgba(128, 128, 128, 0.15); border: 1px solid rgba(128, 128, 128, 0.25); border-radius: 8px; padding: 8px; margin: 4px 0; text-align: center;">
<div style="font-size: 14px; margin-bottom: 4px;"><strong>Did you know?</strong></div>
<div>Pinned context is always included in future chat messages</div>
</div>

Select code & ask me to explain, debug or optimize it, or type `/` for quick actions

</div>

---

@workspace @frontend

## INSTRUÇÃO OBRIGATÓRIA:
Responda a este prompt, incluindo comentários de código e explicações, EXCLUSIVAMENTE em PORTUGUÊS DO BRASIL (PT-BR).

Contexto: O Backend (RF01) está completo e rodando (http://localhost:3000), com rotas para Cursos, Professores, Laboratórios, Disciplinas e Blocos. O Frontend já possui o CRUD de Instituições (`Instituicoes.jsx`) funcionando perfeitamente.

Tarefa: Implementar as telas (CRUDs) restantes do RF01 no Frontend React, mantendo RIGOROSAMENTE o padrão visual e arquitetural do `Instituicoes.jsx`.

1. ATUALIZAR SERVIÇOS (src/services/api.js):
   - Adicione os novos serviços para conectar com o backend:
     - `cursosService` (endpoints: /cursos)
     - `professoresService` (endpoints: /professores)
     - `laboratoriosService` (endpoints: /laboratorios)
     - `disciplinasService` (endpoints: /disciplinas)
     - `blocosService` (endpoints: /blocos)

2. CRIAR COMPONENTES (src/components/):
   Crie as pastas e arquivos .jsx abaixo. Use `Instituicoes.jsx` como template (mesmos imports do MUI, Paper, Table, Dialog, Snackbar, etc).

   - `Cursos/Cursos.jsx`:
     - Tabela: Nome, Instituição (exibir nome), Turnos, Status.
     - Form: 
       - Nome (TextField)
       - Turnos (Select Múltiplo ou Checkbox: &#39;Manhã&#39;, &#39;Tarde&#39;, &#39;Noite&#39;)
       - Instituição (Select: obrigatório carregar lista via `instituicoesService.listar` no `useEffect`)
       - Status (Select Ativo/Inativo)

   - `Professores/Professores.jsx`:
     - Tabela: Nome, Email, Telefone, Status.
     - Form: Nome, Email, Telefone, Status (Select).

   - `Laboratorios/Laboratorios.jsx`:
     - Tabela: Nome, Capacidade, Localização, Status.
     - Form: Nome, Capacidade (type number), Localização, Status.

   - `Disciplinas/Disciplinas.jsx`:
     - Tabela: Nome, Carga Horária, Curso (nome), Professor (nome).
     - Form: 
       - Nome
       - Carga Horária (number)
       - Curso (Select: via `cursosService.listar`)
       - Professor Responsável (Select: via `professoresService.listar`)
       - Status

   - `Blocos/Blocos.jsx` (Blocos de Horário):
     - Tabela: Turno, Dia, Início, Fim, Ordem.
     - Form: 
       - Turno (Select: Manhã, Tarde, Noite)
       - Dia da Semana (Select: 0-Domingo a 6-Sábado)
       - Início (ex: 19:00)
       - Fim (ex: 20:40)
       - Ordem (number)

3. ATUALIZAR MENU (src/components/Menu/Menu.jsx):
   - Importe todos os novos componentes.
   - Adicione os itens na lista do Drawer para cada entidade.
   - Atualize a função de navegação para abrir o componente correto no Modal principal.

Requisitos de Qualidade:
- **Carregamento de Dados:** Ao abrir um modal de criação (ex: Disciplina), certifique-se de buscar os dados necessários (Cursos e Professores) via API dentro de um `useEffect`.
- **Feedback:** Use o `Snackbar` para mensagens de sucesso/erro.
- **UX:** Mantenha o botão de &quot;Novo&quot; e o filtro de busca em todas as telas.

---

Vou implementar as telas restantes do RF01 no Frontend React, seguindo rigorosamente o padrão do `Instituicoes.jsx`. Primeiro, vou analisar a estrutura atual e depois implementar as funcionalidades solicitadas.

---

Agora vou implementar as funcionalidades solicitadas seguindo rigorosamente o padrão do `Instituicoes.jsx`.

## 1. ATUALIZAR SERVIÇOS (src/services/api.js)

---

Adicionando os novos serviços para conectar com o backend: cursosService, professoresService, laboratoriosService, disciplinasService e blocosService

---

## 2. CRIAR COMPONENTES

### 2.1 Cursos/Cursos.jsx

---

Criando pasta para o componente Cursos

---

shell

---

Criando componente Cursos.jsx seguindo o padrão do Instituicoes.jsx

---

### 2.2 Professores/Professores.jsx

---

Criando pasta para o componente Professores

---

shell

---

Criando componente Professores.jsx seguindo o padrão do Instituicoes.jsx

---

### 2.3 Laboratorios/Laboratorios.jsx

---

Criando pasta para o componente Laboratorios

---

shell

---

Criando componente Laboratorios.jsx seguindo o padrão do Instituicoes.jsx

---

### 2.4 Disciplinas/Disciplinas.jsx

---

Criando pasta para o componente Disciplinas

---

shell

---

Criando componente Disciplinas.jsx seguindo o padrão do Instituicoes.jsx

---

### 2.5 Blocos/Blocos.jsx

---

Criando pasta para o componente Blocos

---

shell

---

Criando componente Blocos.jsx seguindo o padrão do Instituicoes.jsx

---

## 3. ATUALIZAR MENU (src/components/Menu/Menu.jsx)

---

Atualizando o Menu.jsx para incluir todos os novos componentes e seus respectivos ícones

---


## Implementação Concluída! 🎉

Implementei com sucesso todas as telas (CRUDs) restantes do RF01 no Frontend React, seguindo rigorosamente o padrão visual e arquitetural do `Instituicoes.jsx`:

### ✅ **1. Serviços Atualizados (api.js)**
- `cursosService` - endpoints para /cursos
- `professoresService` - endpoints para /professores  
- `laboratoriosService` - endpoints para /laboratorios
- `disciplinasService` - endpoints para /disciplinas
- `blocosService` - endpoints para /blocos

### ✅ **2. Componentes Criados**

**Cursos.jsx:**
- Tabela: Nome, Instituição, Turnos (chips), Status
- Form: Nome, Instituição (select), Turnos (select múltiplo), Status
- Carregamento automático de instituições no useEffect

**Professores.jsx:**
- Tabela: Nome, Email, Telefone, Status
- Form: Nome, Email, Telefone, Status (select)

**Laboratorios.jsx:**
- Tabela: Nome, Capacidade, Localização, Status
- Form: Nome, Capacidade (number), Localização, Status

**Disciplinas.jsx:**
- Tabela: Nome, Carga Horária, Curso, Professor, Status
- Form: Nome, Carga Horária (number), Curso (select), Professor (select), Status
- Carregamento automático de cursos e professores no useEffect

**Blocos.jsx:**
- Tabela: Turno, Dia, Início, Fim, Ordem
- Form: Turno (select), Dia da Semana (select 0-6), Início/Fim (time), Ordem (number)

### ✅ **3. Menu Atualizado**
- Adicionados todos os novos itens com ícones apropriados
- Funções de navegação para cada componente
- Integração completa com o sistema de modais

### 🔧 **Características Implementadas:**
- **Padrão Visual:** Idêntico ao Instituicoes.jsx (Paper, Table, Dialog, Snackbar)
- **Carregamento de Dados:** useEffect para buscar dados relacionados (instituições, cursos, professores)
- **Feedback:** Snackbar para mensagens de sucesso/erro em português
- **UX:** Botão "Novo", filtro de busca, confirmação de exclusão
- **Validação:** Campos obrigatórios marcados com *
- **Responsividade:** Layout flexível e adaptável

Todas as telas estão prontas para uso e seguem exatamente o mesmo padrão de qualidade e funcionalidade do CRUD de Instituições já existente!

---

@workspace @frontend

## INSTRUÇÃO OBRIGATÓRIA:
Responda a este prompt EXCLUSIVAMENTE em PORTUGUÊS DO BRASIL (PT-BR).

Contexto: Estamos finalizando o Frontend React (Material UI). Os formulários de CRUD (Instituições, Cursos, etc.) já existem, mas precisamos garantir que a validação e o tratamento de erros sejam robustos, visíveis e à prova de falhas. O usuário nunca deve ficar sem saber o que aconteceu.

Tarefa: Refatorar e aprimorar a lógica de validação e feedback em TODOS os componentes de formulário (`Instituicoes.jsx`, `Cursos.jsx`, `Professores.jsx`, `Laboratorios.jsx`, `Disciplinas.jsx`, `Blocos.jsx`).

Implemente as seguintes camadas de proteção e feedback visual:

1. VALIDAÇÃO VISUAL NOS CAMPOS (MUI):
   - Utilize as props `error` e `helperText` do TextField/Select do Material UI.
   - Se um campo obrigatório estiver vazio ao tentar salvar, ele deve ficar VERMELHO e exibir &quot;Campo obrigatório&quot; logo abaixo dele.
   - Para campos específicos (Email, CNPJ), valide o formato no frontend antes de enviar. Se inválido, mostre erro imediato no campo.

2. MAPEAMENTO DE ERROS DA API (Server-side Feedback):
   - O Backend retorna erros 400/409 com mensagens específicas (ex: &quot;CNPJ já cadastrado&quot;, &quot;Email inválido&quot;).
   - Capture o `error.response.data.message` no `catch` das requisições.
   - Exiba essa mensagem específica no `Snackbar` (barra de aviso) com cor de erro (`severity=&quot;error&quot;`).
   - NÃO mostre mensagens genéricas como &quot;Erro ao salvar&quot; se a API tiver retornado um motivo específico.

3. ESTADOS DE CARREGAMENTO (Loading States):
   - Durante a requisição (Promise pendente):
     - O botão de &quot;Salvar/Criar&quot; deve ficar DESABILITADO (disabled).
     - O texto do botão deve mudar para &quot;Salvando...&quot; ou exibir um `CircularProgress` pequeno dentro dele.
     - Isso impede cliques duplos e duplicidade de dados.

4. MÁSCARAS E FORMATAÇÃO VISUAL:
   - Implemente formatação automática para campos numéricos e de texto fixo:
     - **CNPJ:** Formatar como XX.XXX.XXX/XXXX-XX
     - **Telefone:** Formatar como (XX) XXXXX-XXXX
   - Use o evento `onChange` para aplicar a máscara enquanto o usuário digita.

5. TRATAMENTO DE ERROS DE CONEXÃO:
   - Se o backend estiver offline (Network Error), exiba um Snackbar claro: &quot;Sem conexão com o servidor. Verifique sua internet ou tente novamente mais tarde.&quot;

Aplique essas melhorias em todos os arquivos `.jsx` da pasta `src/components/`. Mantenha o código limpo e modular.

Exemplo de implementação esperada no &#39;submit&#39;:
```javascript
const handleSubmit = async () =&gt; {
  // 1. Validação Local
  if (!formData.nome) {
    setErrors({ ...errors, nome: &#39;Nome é obrigatório&#39; });
    return;
  }

  setLoading(true);
  try {
    // 2. Envio
    await api.post(...)
    mostrarSucesso(&#39;Salvo com sucesso!&#39;);
    fecharModal();
  } catch (error) {
    // 3. Tratamento de Erro API
    const msg = error.response?.data?.message || &#39;Erro desconhecido&#39;;
    mostrarErro(msg); 
  } finally {
    setLoading(false);
  }
}

---

Vou implementar as melhorias de validação e feedback em todos os componentes de formulário. Começarei criando funções utilitárias para validação e formatação, depois aplicarei as melhorias em cada componente.

## 1. Criando Utilitários de Validação e Formatação

---

Criando arquivo de utilitários para validação e formatação

---

Criando pasta utils

---

shell

---

Criando arquivo de utilitários para validação e formatação

---

## 2. Refatorando Instituicoes.jsx

---

Refatorando Instituicoes.jsx com validação robusta, estados de carregamento e formatação

---

## 3. Refatorando Cursos.jsx

---

Refatorando Cursos.jsx com validação robusta e estados de carregamento

---

## 4. Refatorando Professores.jsx

---

Refatorando Professores.jsx com validação robusta e estados de carregamento

---

## 5. Refatorando Laboratorios.jsx

---

Refatorando Laboratorios.jsx com validação robusta e estados de carregamento

---

## 6. Refatorando Disciplinas.jsx

---

Refatorando Disciplinas.jsx com validação robusta e estados de carregamento

---

## 7. Refatorando Blocos.jsx

---

Refatorando Blocos.jsx com validação robusta e estados de carregamento

---


## Implementação Concluída! 🎉

Refatorei com sucesso **TODOS** os componentes de formulário do Frontend React com validação robusta e feedback visual completo:

### ✅ **1. Utilitários de Validação (src/utils/validation.js)**
- `validarEmail()` - Validação de formato de email
- `validarCNPJ()` - Validação de CNPJ (14 dígitos)
- `formatarCNPJ()` - Formatação automática XX.XXX.XXX/XXXX-XX
- `formatarTelefone()` - Formatação automática (XX) XXXXX-XXXX
- `tratarErroAPI()` - Tratamento inteligente de erros de rede e API

### ✅ **2. Validação Visual nos Campos (Material UI)**
- **Campos obrigatórios:** Ficam vermelhos com mensagem "Campo obrigatório"
- **Validação de formato:** Email e CNPJ validados em tempo real
- **Limpeza de erros:** Erros desaparecem quando o usuário corrige o campo
- **Props error/helperText:** Implementadas em todos os TextField e Select

### ✅ **3. Mapeamento de Erros da API**
- **Captura específica:** `error.response?.data?.message` capturado corretamente
- **Tratamento de rede:** Mensagens claras para problemas de conexão
- **Snackbar inteligente:** Exibe mensagens específicas da API, não genéricas

### ✅ **4. Estados de Carregamento**
- **Botão desabilitado:** Durante salvamento para evitar cliques duplos
- **Texto dinâmico:** "Salvando..." com CircularProgress
- **Estado saving:** Controla toda a interface durante operações

### ✅ **5. Máscaras e Formatação**
- **CNPJ:** Formatação automática enquanto digita
- **Telefone:** Formatação (XX) XXXXX-XXXX em tempo real
- **Números:** Validação de valores mínimos (capacidade > 0, etc.)

### ✅ **6. Validações Específicas por Componente**

**Instituições:**
- Nome, Sigla, CNPJ obrigatórios
- CNPJ com validação de formato
- Email com validação de formato

**Cursos:**
- Nome e Instituição obrigatórios
- Select de Instituição com validação

**Professores:**
- Nome e Email obrigatórios
- Email com validação de formato

**Laboratórios:**
- Nome e Capacidade obrigatórios
- Capacidade deve ser > 0

**Disciplinas:**
- Nome, Carga Horária, Curso e Professor obrigatórios
- Carga Horária deve ser > 0

**Blocos:**
- Todos os campos obrigatórios
- Validação de horários (fim > início)
- Ordem deve ser > 0

### 🔧 **Características Implementadas:**
- **Feedback Imediato:** Erros aparecem instantaneamente nos campos
- **UX Intuitiva:** Usuário sempre sabe o que está acontecendo
- **Prevenção de Erros:** Validação antes do envio + máscaras automáticas
- **Robustez:** Tratamento completo de erros de rede e API
- **Performance:** Estados de loading previnem operações duplicadas

O sistema agora está **à prova de falhas** com validação completa em todas as camadas!