<div style="display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; padding: 200px 0 20px 0;">

<div style="font-size: 24px; margin-bottom: 12px;"><strong>Amazon Q</strong></div>
<div style="background: rgba(128, 128, 128, 0.15); border: 1px solid rgba(128, 128, 128, 0.25); border-radius: 8px; padding: 8px; margin: 4px 0; text-align: center;">
<div style="font-size: 14px; margin-bottom: 4px;"><strong>Did you know?</strong></div>
<div>Compact your conversation with /compact</div>
</div>

Select code & ask me to explain, debug or optimize it, or type `/` for quick actions

</div>

---

@workspace @backend

## INSTRUÇÃO OBRIGATÓRIA:
Responda a este prompt, incluindo todas as explicações, comentários de código e documentação, EXCLUSIVAMENTE em PORTUGUÊS DO BRASIL (PT-BR).

---

Contexto: Estamos finalizando o Trabalho Final (PM2025-2). O RF01 exige CRUD de Instituição, Curso, Professor, Disciplina, Laboratório e Blocos. Atualmente, apenas &quot;Instituicao&quot; está implementado. Precisamos implementar o restante do Backend mantendo estritamente o padrão de arquitetura atual (Controller, Routes, Model, Swagger).

Tarefa: Implementar as entidades restantes do RF01 no Backend Node.js/Mongoose.

1. CORREÇÃO DE MODELO:
   - Atualize `src/models/Instituicao.js`: Adicione o campo `sigla` (String, required, uppercase) que está faltando conforme requisitos.

2. NOVOS MODELOS (Mongoose):
   Crie os arquivos em `src/models/` com os seguintes campos (conforme documento de requisitos):
   
   - `Curso.js`:
     - instituicao: { type: mongoose.Schema.Types.ObjectId, ref: &#39;Instituicao&#39;, required: true }
     - nome: { type: String, required: true }
     - turnos: { type: [String], required: true } // Ex: [&#39;Manhã&#39;, &#39;Noite&#39;]
     - status: { type: String, enum: [&#39;ativo&#39;, &#39;inativo&#39;], default: &#39;ativo&#39; }
   
   - `Professor.js`:
     - nome: { type: String, required: true }
     - email: { type: String, required: true, unique: true }
     - telefone: String
     - status: { type: Boolean, default: true }
   
   - `Laboratorio.js`:
     - nome: { type: String, required: true } (Identificador)
     - capacidade: { type: Number, required: true }
     - localizacao: String
     - status: { type: Boolean, default: true }

   - `Disciplina.js`:
     - curso: { type: mongoose.Schema.Types.ObjectId, ref: &#39;Curso&#39;, required: true }
     - nome: { type: String, required: true }
     - cargaHoraria: { type: Number, required: true }
     - professorResponsavel: { type: mongoose.Schema.Types.ObjectId, ref: &#39;Professor&#39; }
     - status: { type: Boolean, default: true }

   - `Bloco.js` (Blocos de horário):
     - turno: { type: String, required: true }
     - diaSemana: { type: Number, required: true } (0-6 ou 1-7)
     - inicio: { type: String, required: true } (Ex: &quot;19:00&quot;)
     - fim: { type: String, required: true } (Ex: &quot;20:40&quot;)
     - ordem: { type: Number, required: true }

3. CAMADA DE CONTROLLERS E ROTAS:
   - Para CADA modelo acima, crie o respectivo Controller em `src/controllers/` com:
     - criar (POST)
     - listar (GET) com paginação e filtros simples
     - atualizar (PUT)
     - remover (DELETE)
   - Crie as rotas em `src/routes/` seguindo o padrão de `instituicoes.js`.
   - Inclua documentação Swagger (@swagger) em todas as rotas.

4. INTEGRAÇÃO:
   - Atualize `src/models/index.js` exportando todos os novos modelos.
   - Atualize `server.js` registrando as novas rotas:
     - /api/v1/cursos
     - /api/v1/professores
     - /api/v1/laboratorios
     - /api/v1/disciplinas
     - /api/v1/blocos

Requisitos de Qualidade:
- Use tratamento de erros centralizado (next(error)).
- Mantenha validações de campos obrigatórios.
- As mensagens de erro e sucesso da API devem ser em pt-BR.

---

Vou implementar todas as entidades restantes do RF01 no backend, seguindo o padrão arquitetural existente. Primeiro, vou analisar a estrutura atual e depois implementar cada componente.

---

Agora vou implementar todas as entidades restantes do RF01. Começarei corrigindo o modelo de Instituição para adicionar o campo `sigla`, depois criarei todos os novos modelos, controllers e rotas.

## 1. CORREÇÃO DO MODELO INSTITUIÇÃO

---

Adicionando o campo sigla obrigatório ao modelo de Instituição conforme requisitos

---

## 2. CRIAÇÃO DOS NOVOS MODELOS

### Modelo Curso

---

Criando o modelo Curso com referência à Instituição

---

### Modelo Professor

---

Criando o modelo Professor com validação de email único

---

### Modelo Laboratório

---

Criando o modelo Laboratório com validação de capacidade

---

### Modelo Disciplina

---

Criando o modelo Disciplina com referências a Curso e Professor

---

### Modelo Bloco

---

Criando o modelo Bloco com validação de horários e dias da semana

---

## 3. CRIAÇÃO DOS CONTROLLERS

### Controller de Curso

---

Criando o controller de Curso com populate da instituição

---

### Controller de Professor

---

Criando o controller de Professor com filtros por nome e email

---

### Controller de Laboratório

---

Criando o controller de Laboratório com filtro por capacidade

---

### Controller de Disciplina

---

Criando o controller de Disciplina com populate de curso e professor

---

### Controller de Bloco

---

Criando o controller de Bloco com ordenação por dia da semana e ordem

---

## 4. CRIAÇÃO DAS ROTAS

### Rotas de Curso

---

Criando as rotas de Curso com documentação Swagger completa

---

### Rotas de Professor

---

Criando as rotas de Professor com documentação Swagger completa

---

### Rotas de Laboratório

---

Criando as rotas de Laboratório com documentação Swagger completa

---

### Rotas de Disciplina

---

Criando as rotas de Disciplina com documentação Swagger completa

---

### Rotas de Bloco

---

Criando as rotas de Bloco com documentação Swagger completa

---

## 5. ATUALIZAÇÃO DOS ARQUIVOS DE INTEGRAÇÃO

### Atualização do index.js dos modelos

---

Atualizando o index.js para exportar todos os novos modelos

---

### Atualização do server.js

---

Adicionando as importações das novas rotas no server.js

---

Registrando todas as novas rotas no server.js

---

### Atualização da documentação Swagger nas rotas de Instituição

---

Adicionando o campo sigla na documentação Swagger de Instituição

---

Atualizando os campos obrigatórios na documentação Swagger de Instituição

---


## Implementação Concluída! 

Implementei com sucesso todas as entidades restantes do RF01 no backend, seguindo rigorosamente o padrão arquitetural existente:

### ✅ **1. CORREÇÃO DO MODELO INSTITUIÇÃO**
- Adicionado campo `sigla` (String, required, uppercase) conforme requisitos

### ✅ **2. NOVOS MODELOS CRIADOS**
- **Curso**: Com referência à Instituição, turnos e status
- **Professor**: Com email único e validação
- **Laboratório**: Com capacidade e localização
- **Disciplina**: Com referências a Curso e Professor
- **Bloco**: Com validação de horários e dias da semana

### ✅ **3. CONTROLLERS IMPLEMENTADOS**
Todos os controllers seguem o padrão existente com:
- Operações CRUD completas (criar, listar, atualizar, remover)
- Paginação e filtros específicos para cada entidade
- Populate automático para relacionamentos
- Tratamento de erros centralizado
- Mensagens em português brasileiro

### ✅ **4. ROTAS CONFIGURADAS**
- Documentação Swagger completa para todas as rotas
- Filtros específicos para cada entidade:
  - **Cursos**: status, nome, instituição
  - **Professores**: status, nome, email
  - **Laboratórios**: status, nome, capacidade mínima
  - **Disciplinas**: status, nome, curso, professor
  - **Blocos**: turno, dia da semana

### ✅ **5. INTEGRAÇÃO COMPLETA**
- Todos os modelos exportados em `src/models/index.js`
- Todas as rotas registradas no `server.js`:
  - `/api/v1/cursos`
  - `/api/v1/professores`
  - `/api/v1/laboratorios`
  - `/api/v1/disciplinas`
  - `/api/v1/blocos`

### **Endpoints Disponíveis:**
Cada entidade possui os 4 endpoints padrão:
- `POST /api/v1/{entidade}` - Criar
- `GET /api/v1/{entidade}` - Listar (com filtros e paginação)
- `PUT /api/v1/{entidade}/:id` - Atualizar
- `DELETE /api/v1/{entidade}/:id` - Remover

### **Funcionalidades Especiais:**
- **Relacionamentos**: Cursos vinculados a Instituições, Disciplinas a Cursos e Professores
- **Validações**: Emails únicos, horários válidos, capacidades positivas
- **Filtros Inteligentes**: Busca por texto, status, relacionamentos
- **Documentação**: Swagger completo para todas as APIs

O backend agora está completo com todas as entidades do RF01, mantendo a qualidade e padrão arquitetural estabelecido!